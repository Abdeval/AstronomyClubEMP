import { AlertDialog } from '@/components/ui/alert-dialog'
import { AlertDialogContent, AlertDialogTrigger } from '@radix-ui/react-alert-dialog'
import React from 'react'

// ! this is the profile page whenever you want to add something create a component in the 
// ! component/profile dir and import it to use it here 

export default function Profile() {
  return (
    <div className='text-foreground'>
      profile
    </div>
  )
}
