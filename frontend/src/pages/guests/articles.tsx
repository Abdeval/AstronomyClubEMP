// import ParallaxComponent from "@/components/landing/parallax"

// write all the logic of the page here no commponents just in this file 
// as you can see in the nav bar there is the change the button so make sure 
// the design is responsive and the dark mode is good also

const Articles = () => {
  return (
    <div className="h-full w-full bg-background text-foreground">
      articles
    </div>
  )
}

export default Articles